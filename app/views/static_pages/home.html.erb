<%= form_tag("/", method: "post") do %>
  <%= label_tag(:id, "User ID:") %>
  <%= text_field_tag(:id) %>
  <%= submit_tag("Search") %>
<% end %>
<% unless @photos.nil? %>
  <% @photos.each do |photo| %>
    <%= image_tag "#{photo.medium!(500).source_url}" %>
  <% end %>
<% end %>
